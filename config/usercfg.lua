local vrs = require('config.defcfg')
return {
  themeDir = vrs.themeDir,
  iconDir = vrs.iconDir,
  pfpDir = vrs.pfpDir, 
  userName = vrs.userName,
  rofiEmoji = vrs.rofiEmoji, 
  rofiRun = vrs.rofiRun,
  rofi = vrs.rofi,
  scr = vrs.scr,
  scrDelay = vrs.scrDelay,
  str = vrs.str,
  downDir = vrs.downDir,
  docsDir = vrs.docsDir,
  msicDir = vrs.msicDir,
  picsDir = vrs.picsDir,
  vidsDir = vrs.vidsDir,
  repoDir = vrs.repoDir,
  searchDDG = vrs.searchDDG,
  searchARW = vrs.searchARW,
  launchSTM = vrs.launchSTM
}
